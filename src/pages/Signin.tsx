/**
 * Signin Container를 사용하고 Container에서 Redux와 연결하고
 * 하위 컴포넌트로 Presenter를 만듦
 */
import React from "react";
import SigninContainer from "../containers/SigninContainer";

export default function Signin() {
  return <SigninContainer />;
}
